<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orosirian.trade.user.db.mappers.UserMapper">

    <insert id="insertUser">
        INSERT
        INTO user(user_name, login_password, tags)
        VALUES (#{userName}, #{loginPassword}, #{tags})
    </insert>

    <select id="selectLoginPassword" resultType="java.lang.String">
        SELECT login_password
        FROM user
        WHERE user_name = #{userName}
    </select>

    <select id="isUserNameExist" resultType="java.lang.Boolean">
        SELECT COUNT(1) > 0
        FROM user
        WHERE user_name = #{userName}
    </select>

</mapper>