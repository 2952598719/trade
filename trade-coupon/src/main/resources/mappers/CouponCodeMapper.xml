<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orosirian.trade.coupon.db.mappers.CouponCodeMapper">
    <resultMap id="BaseResultMap" type="com.orosirian.trade.coupon.db.model.CouponCode">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="code" jdbcType="VARCHAR" property="code" />
        <result column="batch_id" jdbcType="BIGINT" property="batchId" />
        <result column="user_id" jdbcType="BIGINT" property="userId" />
        <result column="status" jdbcType="INTEGER" property="status" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
        <result column="modify_time" jdbcType="TIMESTAMP" property="modifyTime" />
    </resultMap>
    <sql id="Base_Column_List">
        id, code, batch_id, user_id, status, create_time, modify_time
    </sql>

    <insert id="insertCouponCode" parameterType="com.orosirian.trade.coupon.db.model.CouponCode">
        INSERT
        INTO coupon_code(code, batch_id, user_id, status, create_time, modify_time)
        VALUES (#{code}, #{batchId}, #{userId}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="queryCouponCodeByCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM coupon_code
        WHERE code = #{code}
    </select>

    <update id="updateCouponCode" parameterType="com.orosirian.trade.coupon.db.model.CouponCode">
        UPDATE coupon_code
        <set>
            <if test="code != null">
                code = #{code,jdbcType=VARCHAR},
            </if>
            <if test="batchId != null">
                batch_id = #{batchId,jdbcType=BIGINT},
            </if>
            <if test="userId != null">
                user_id = #{userId,jdbcType=BIGINT},
            </if>
            <if test="status != null">
                status = #{status,jdbcType=INTEGER},
            </if>
            <if test="modifyTime != null">
                modify_time = #{modifyTime,jdbcType=TIMESTAMP},
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="queryCouponCodeByBatch" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM coupon_code
        WHERE batch_id = #{batchId}
    </select>

</mapper>